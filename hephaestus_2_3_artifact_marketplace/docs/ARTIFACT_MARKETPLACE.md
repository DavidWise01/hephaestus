# Hephaestus v2.3 Artifact Marketplace

Adds artifact package manifests, catalog, version registry, install/uninstall flow, dependency lockfile, trust scoring, signature placeholders, compatibility checks, search index, and marketplace dashboard.
